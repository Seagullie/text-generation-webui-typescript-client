const PORT = 5000
const HOST = `localhost:${PORT}`

// endpoint URIs
export const CHAT_URI = `http://${HOST}/api/v1/chat`
export const MODEL_URI = `http://${HOST}/api/v1/model`
export const GENERATE_URI = `http://${HOST}/api/v1/generate`
